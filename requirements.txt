# Esquema y validador.
PyYAML==6.0.3

# Solo para `validar.py --schema` (contraste con el JSON Schema).
jsonschema==4.26.0

# Tests.
pytest==9.1.1

# Parser: capa derivada tidy -> parquet y SQLite (SQLite va en la stdlib).
pandas==3.0.5
pyarrow==25.0.1
